Inline error stated as direction (what failed + what to do) with one retry. Soft danger fill, never a full-bleed alarm.

```jsx
<ErrorBanner message="Couldn't reach that link." actionLabel="Try again" onAction={retry} onDismiss={hide} />
```
