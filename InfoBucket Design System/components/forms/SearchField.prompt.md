Free-text search well (sunken surface) with leading magnifier and a clear ×. Use on Search and inside bucket detail for internal filtering.

```jsx
<SearchField value={q} onChange={setQ} onClear={() => setQ('')} placeholder="Search everything" />
```
