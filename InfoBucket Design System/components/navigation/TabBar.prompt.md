Bottom tab bar with Inbox · Library · Search. Fixed to the bottom on device, active tab in primary color, optional count badges.

```jsx
<TabBar active="inbox" badge={{ inbox: 3 }} onChange={setTab} />
```

Pair with `AddButton` floated above it for manual capture.
