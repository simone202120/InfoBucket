# L'ecosistema React 19 / Expo SDK 54 ha peer dependency in conflitto su npm
# (es. react-server-dom-webpack vs expo-router). legacy-peer-deps consente a
# `npm install` e `expo install` di risolvere come fanno gli altri package manager.
legacy-peer-deps=true
